# Runtime dependencies for the NeuroMotorica package.
# Keeping this file aligned with pyproject.toml ensures CI installs
# everything needed for tests without requiring an editable install.
numpy>=1.24
matplotlib>=3.8
typer>=0.12
rich>=13.0
pyttsx3>=2.90
pydantic>=2.7
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
