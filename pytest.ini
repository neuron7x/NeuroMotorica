[pytest]
# Точки запуску тестів
testpaths = tests
# Патерни тестових файлів
python_files = test_*.py *_test.py
# Додаткові опції, зручні для CI
addopts = -ra -q
markers =
    slow: marks tests as slow (deselect with -m "not slow")
